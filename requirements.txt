
# =====================================================================
# Smart Rent Car — Internal CRM
# Python 3.13  |  Django 6.1
# Base pins verified against the working interpreter on 2026-09-23.
# =====================================================================

# --- Core ---
Django==6.1.1

# --- Settings / secrets (python-decouple) ---
# config/settings/base.py does `from decouple import Csv, config`.
python-decouple==3.8

# --- Database driver ---
# PostgreSQL is used in BOTH dev and prod (needed for the booking
# range-exclusion constraint that prevents overbooking).
psycopg[binary]==3.3.6

# --- i18n model fields (FR / AR / EN) ---
# "modeltranslation" is listed in INSTALLED_APPS; each app exposes translation.py.
django-modeltranslation==0.20.6

# --- Images (vehicles.Car.picture, vehicles.CarImage.image) ---
pillow==12.3.0

# --- Invoice PDF rendering (invoicing app) ---
weasyprint==69.0

# --- Bookings list export (.xlsx) ---
openpyxl==3.1.5

# --- Production only — pin exact versions at deploy time ------------
# gunicorn
# whitenoise

# --- DELIBERATELY DROPPED -------------------------------------------
# djangorestframework==3.18.1
#   Installed, but currently 0 lines of usage (no serializers, no API views).
#   Recommendation: remove from INSTALLED_APPS and from this file.
#   Re-add only when a real API consumer (mobile app / partner integration) exists.
